{
  "name": "@speconn/typespec-speconn-node",
  "version": "0.0.10",
  "type": "module",
  "main": "dist/src/index.js",
  "typespec": {
    "emitter": true,
    "options": {
      "@speconn/typespec-speconn-node": {
        "type": "object",
        "properties": {
          "emitter-output-dir": { "type": "string" }
        },
        "required": ["emitter-output-dir"]
      }
    }
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@typespec/compiler": "1.11.0",
    "@typespec/http": "1.11.0",
    "typescript": "^5.8.0"
  }
}
